/// <summary>
/// Действие, выполняемое системой таргетинга при срабатывании условия выхода из пайплайна.
/// Реализации могут изменять статус события, останавливать пайплайн, диспатчить новые события и т.д.
/// </summary>
public interface ITargetingAction
{
    void Execute(TargetingContext context);
}
